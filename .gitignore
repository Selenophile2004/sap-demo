node_modules/
dist/
*.tsbuildinfo

# اسرار محلی — این‌ها فقط باید در پنل Render (Environment Variables) ست شوند،
# هرگز در گیت‌هاب/مخزن عمومی commit نشوند. .env.production عمداً استثناست:
# فقط مسیر نسبی API رو مشخص می‌کنه (بدون هیچ رمز/کلیدی)، و باید کامیت بشه چون
# موقع build فرانت‌اند روی Render همین فایل باید وجود داشته باشه.
.env
.env.local
!.env.production

# دیتای runtime بک‌اند (کامنت‌ها/گروه‌بندی‌های دستی) — این‌ها باید روی هر
# استقرار از نو و خالی ساخته شوند، نه از نسخه‌ی لوکال کپی شوند.
backend/data/
